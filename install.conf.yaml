- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/dotfiles: ''
    ~/.config/nvim: nvim
    ~/.Xdefaults: Xdefaults 
    ~/.config/awesome: awesome

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [nvim +PlugInstall +all, Install neovim plugins]
